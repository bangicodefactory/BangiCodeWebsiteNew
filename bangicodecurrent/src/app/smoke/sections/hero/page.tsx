import { HeroSection } from "@/components/sections/HeroSection";

export default function SmokeHeroPage() {
  return (
    <main className="bg-background min-h-screen">
      <HeroSection />
    </main>
  );
}
